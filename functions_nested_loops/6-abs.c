#include "main.h"
/**
 * _abs - prints the absolute value of the integer
 * @n: the input 
 *
 * Return: 1 if letter, else 0
 *
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}
