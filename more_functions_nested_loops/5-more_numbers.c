#include "main.h"
/**
 * more_numbers - The Main Function
 * @void: no input
 *
 * Return: the number
 */

void more_numbers(void)
{
	int i;
	int c;
	int tens;
	int ones;

	for (i = 0; i < 10: i++)
	{
		for (c = 0; c < 15; c++)
		{	
			if ( c < 10)
				ones = c;
			else
				_putchar('0' + c);
			_putchar();//10
			_putchar();//0
		}
		_putchar('\n');
	}
}
